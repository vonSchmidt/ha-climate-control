#!/usr/bin/env bash
# mypy and pytest require a Python 3.12 env with homeassistant installed — covered by CI.
set -euo pipefail

echo "==> ruff lint"
python -m ruff check custom_components/ tests/

echo "==> ruff format"
python -m ruff format --check custom_components/ tests/

echo "All checks passed."
